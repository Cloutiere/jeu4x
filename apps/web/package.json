{
  "name": "@game/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client SPA (Svelte 5, build statique prêt pour Cloudflare Pages — pas de SSR).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "sync-art": "node scripts/sync-art.mjs"
  },
  "dependencies": {
    "@game/rules": "workspace:*",
    "@game/shared": "workspace:*",
    "pixi.js": "^8.20.1",
    "svelte": "^5.0.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "@types/three": "^0.185.4",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
