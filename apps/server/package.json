{
  "name": "@game/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker Cloudflare + Durable Objects (GameDO, LobbyDO) + OAuth. Voir README.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@game/rules": "workspace:*",
    "@game/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.127.0"
  }
}
